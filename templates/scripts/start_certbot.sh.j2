#!/bin/bash

argInput=$@

/usr/bin/docker run  --rm --name certbot \
{% for volume in certbot_all_volumes %}
  -v {{ volume }} \
{% endfor %}
  {{ certbot_container_image }} $argInput \
    --config-dir /certbot/config --work-dir /certbot/work --logs-dir /certbot/logs

